#include "../inc/test.h"
#include "../inc/global.h"
using namespace std;

// #define INFO
// #define APR     1

#ifdef INFO
static string apr_idea = "";
static string time_cmplx = "";
static string space_cmplx = "";
#endif

Debug db = Debug{true}; // Turn on/off debug mode

/**
 * Problem {num}: {name}
 * LeetCode: {leetcode}
 * @input:
 * @output:
 */
 class Solution {
public:
    Solution() {
        #ifdef INFO
            #if APR == 1
                apr_idea = "";
                time_cmplx = "";
                space_cmplx = "";
            #elif APR == 2
                apr_idea = "";
                time_cmplx = "";
                space_cmplx = "";
            #endif
        #endif
    }

    // Implement solution below

};


void test{num}() {
    struct Case {
        // add fields
    };

    vector<Case> cases = {
        // {{}}
    };

    Solution sol = Solution();

    #ifdef INFO
    cout << "--------------------------------" << endl;
    cout << "\033[1m\033[34mApproach " << APR << ": " << apr_idea << "\033[0m" << endl;
    cout << "  Time Complexity: O(" << time_cmplx << ")" << endl;
    cout << "  Space Complexity: O(" << space_cmplx << ")" << endl;
    #endif

    for (int i = 0; i < (int)cases.size(); ++i) {
        Case c = cases[i];
        // Inputs
        auto in1 = c.

        // Result
        auto res = sol.

        if(assertTest(res, c.exp, i) == false) {
            cout << "   Input    : " << c.in1;
        }
    }
}
